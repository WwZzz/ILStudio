# Optimizer/runtime fields shared with train.py through ConfigLoader.load_training.
# RL loop, rollout, batching, logging, and checkpoint cadence stay in configs/rl.
learning_rate: 0.000001
weight_decay: 0.0
optim: adamw_torch
adam_beta1: 0.9
adam_beta2: 0.999
adam_epsilon: 1e-8
seed: 0
